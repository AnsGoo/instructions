[project]
name = "ext_model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.5",
    "django-filter>=25.1",
    "djangorestframework>=3.16.1",
]

[tool.setuptools]
packages = ["ext_model"]
package-dir = {"ext_model" = "src"}

[tool.uv]
package = true

